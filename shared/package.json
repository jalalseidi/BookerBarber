{
  "name": "@barberbooker/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for BarberBooker",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prepare": "npm run clean && npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^4.9.5",
    "rimraf": "^3.0.2"
  }
}